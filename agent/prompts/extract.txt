You are an expert meeting analyst. Extract all action items from the meeting transcript below.

Return ONLY a valid JSON array. Each item must have exactly these fields:
- "description": string — what needs to be done (non-empty)
- "assignee": string — the person responsible (non-empty, use "Unassigned" if unclear)
- "due_date": string or null — ISO date format YYYY-MM-DD if mentioned, otherwise null

Example output:
[
  {"description": "Send project proposal to client", "assignee": "Alice", "due_date": "2024-02-15"},
  {"description": "Review budget spreadsheet", "assignee": "Bob", "due_date": null}
]

If there are no action items, return an empty array: []
Do not include any explanation or text outside the JSON array.
